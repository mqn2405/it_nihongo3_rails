<%= form_with(model: bookreview, local: true) do |form| %>
  <% if bookreview.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookreview.errors.count, "error") %> prohibited this bookreview from being saved:</h2>

      <ul>
        <% bookreview.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.number_field :book_id %>
  </div>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_area :review %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
