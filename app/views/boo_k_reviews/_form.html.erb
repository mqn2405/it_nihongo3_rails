<%= form_with(model: boo_k_review, local: true) do |form| %>
  <% if boo_k_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boo_k_review.errors.count, "error") %> prohibited this boo_k_review from being saved:</h2>

      <ul>
        <% boo_k_review.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.number_field :book_id %>
  </div>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_area :review %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
